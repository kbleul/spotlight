const Loading = () => {
  return (
    <main className="relative h-[100vh] w-full bg-gradient-to-b from-[#292929] to-[#000] flex flex-col justify-center items-center"></main>
  );
};

export default Loading;
